# Rust build output
/target

# Local secrets & config — NEVER commit the real token
.env

# Compiled benchmark helper
/bench/loadgen

# Runtime logs
*.out
picodb.out

# AOF persistence files (data — never commit)
*.aof
*.aof.tmp
picodb.aof*

# Python bytecode
__pycache__/
*.pyc

# Personal / secret-bearing launchers (never commit tokens)
start_bot.sh
examples/telegram_bot.py

# Editor/OS noise
*.swp
.DS_Store
